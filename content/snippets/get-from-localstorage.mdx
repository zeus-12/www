---
title: getFromLocalStorage
description: Get data stored in the local storage with a fallback to null
date: 2021-12-24
---

Avoid error if window has not loaded

## Code

```tsx:getFromLocalStorage.tsx
export function getFromLocalStorage(key) {
  if (typeof localStorage !== 'undefined') {
    return localStorage.getItem(key);
  }
  return null;
}
```

## Usage

```js
const [token, setToken] = useState(() => getFromLocalStorage('token') || null);
```
